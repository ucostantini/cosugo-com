import { GovukInitDirective } from './govuk-init-directive';

describe('GovukInitDirective', () => {
  it('should create an instance', () => {
    const directive = new GovukInitDirective();
    expect(directive).toBeTruthy();
  });
});
